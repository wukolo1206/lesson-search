# 學生個人化測驗系統（確認版）

## 確認需求摘要

| 項目 | 設定 |
|---|---|
| 學生人數 | 10 人，各用平板 |
| 登入方式 | 學生**自行輸入**座號 + 姓名 |
| 出題方式 | 老師指派文章，出該文章所有遷移題 |
| 作答後回饋 | **即時批改**：送出後看到每題對錯 + 正確答案 |
| 重複作答 | **可以**，覆蓋最新一次紀錄 |
| 老師看板 | 今日 + **歷史紀錄** |
| 介面語言 | 全中文（學生端不顯示認知歷程）|
| Sheets ID | `1IDu-J5luPJsKA5O7UPtiXhnueQ_JNZJ2ccdnldCCpdI`（沿用現有）|

---

## 系統架構

```
老師端 teacher.html
  └─ 指派文章（setAssignment）
  └─ 查看成績看板（getResults）：今日 / 歷史

       ↕ GAS API

  Google Sheets（現有 + 新增）
  ├─ 學習遷移題目（現有）
  ├─ 今日指派（新增）：文章標題
  └─ 作答紀錄（新增）：時間 | 座號 | 姓名 | 文章 | 題號 | 學生答 | 正確答 | 是否正確

       ↕ GAS API

學生端 quiz.html
  └─ 輸入座號 + 姓名 → 登入
  └─ 閱讀文章 + 作答所有題目
  └─ 送出 → 即時顯示對錯 + 正確答案
```

---

## 新增 Google Sheets 工作表（由初始化 GAS 函式自動建立）

### `今日指派`
| 文章標題 |
|---|
| （GAS 自動填入）|

### `作答紀錄`（系統自動寫入，重複作答覆蓋舊紀錄）
| 時間戳 | 座號 | 姓名 | 文章標題 | 題號 | 學生答案 | 正確答案 | 是否正確 |
|---|---|---|---|---|---|---|---|

---

## 程式碼變更

### [MODIFY] [Code.js](file:///d:/test%20ch/閱讀教學策略查詢系統/Code.js)

新增 5 個函式：

| 函式 | 說明 |
|---|---|
| `initSheets()` | 一次性初始化：建立「今日指派」和「作答紀錄」工作表 |
| `getAssignment()` | 取得今日指派的文章 + 所有遷移題 |
| `submitAnswers(payload)` | 儲存作答（覆蓋同學生今日舊答），回傳批改結果 |
| `setAssignment(title)` | 老師指派文章（寫入今日指派） |
| `getResults(dateFilter)` | 讀取作答紀錄（`today` 或 `all`） |

---

### [NEW] `quiz.html` — 學生測驗頁

**流程**：登入（座號 + 姓名）→ 閱讀文章 + 作答 → 即時批改結果

- 大字體、平板友善
- 文章在上，題目在下（單頁捲動）
- 結果頁：每題 ✅ 或 ❌ + 顯示正確答案

---

### [NEW] `teacher.html` — 老師管理頁

1. **指派區**：下拉選單選文章 → 按「指派給全班」
2. **成績看板**：
   - 切換「今日」/ 「歷史」
   - 表格：每位學生每題對錯（✅/❌）
   - 班級平均答對率

---

## 驗證計畫

| 測試 | 預期結果 |
|---|---|
| 老師指派文章 | Sheets 更新，學生端看到新文章 |
| 學生登入作答 | 即時顯示對錯，Sheets 寫入紀錄 |
| 重複作答 | 舊紀錄被覆蓋，保留最新 |
| 未指派時登入 | 顯示「今天還沒有指派測驗，請稍候」|
| 老師看歷史 | 顯示所有日期的作答紀錄 |

---

## 部署

```powershell
cd "d:\test ch\閱讀教學策略查詢系統"
clasp push
# 再至 GAS 頁建立新版本部署
```
